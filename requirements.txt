# Core
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
httpx>=0.27.0
tqdm>=4.66.0

# ML / Embeddings
torch>=2.5.0
sentence-transformers>=4.1.0
transformers>=4.47.0
numpy>=2.0.0
datasets>=3.6.0
einops>=0.8.0

# ONNX (reranker acceleration)
onnxruntime>=1.20.0
optimum-onnx>=0.1.0

# Vector search
qdrant-client>=1.12.0
scikit-learn>=1.5.0

# Azure
azure-core>=1.32.0
azure-identity>=1.19.0
azure-storage-queue>=12.9.0
azure-storage-blob>=12.19.0

# ETL text hygiene
# ftfy repairs mojibake in the source dataset (26% of rows arrived as UTF-8
# decoded as cp1252); langdetect keeps the index to the language the embedding
# model and reranker actually handle.
ftfy>=6.3.0
langdetect>=1.0.9

# Query Understanding
symspellpy>=6.7.7

# Evaluation
# (no extra deps — uses stdlib + above)
