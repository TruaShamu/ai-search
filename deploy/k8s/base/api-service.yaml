apiVersion: v1
kind: Service
metadata:
  name: booksearch-api
  labels:
    app: booksearch-api
spec:
  type: ClusterIP
  selector:
    app: booksearch-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
